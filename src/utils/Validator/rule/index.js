export const DEFAULT_RULE = {
    isRquired : true,
}

export const NAME_RULE = {
    isRquired: true,
    minLength: 3,
}

export const PHONE_RULE = {
    isRquired: true,
    maxLength: 15,
    minLength: 10,
}

export const EMAIL_RULE = {
    isRquired: true,
    isEMail : true
}