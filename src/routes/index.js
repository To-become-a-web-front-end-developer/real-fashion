import { BrowserRouter, Route } from "react-router-dom"

export const Routes = () => {
    return(
       <BrowserRouter>
        <Routes>
            {/* <Route element={</>}/> */}
        </Routes>
       </BrowserRouter>
    )
}